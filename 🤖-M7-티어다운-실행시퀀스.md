# 🤖 [MMT] M7 인프라 티어다운 — 실행 시퀀스 (사용자 맥에서 실행)

> 상위 런북 = [`🤖-M7-인프라-티어다운-재런치.md`](🤖-M7-인프라-티어다운-재런치.md). 이 문서는 **실제 실행 명령을 게이트와 함께 순서대로** 담은 실행분이다.
> 목적: 비용 절감을 위해 프로덕션 인프라를 **완전 종료($0)**하되, 재런치 가능하게 데이터·상태를 보존한다.
> ⚠️ **비가역**(RDS 삭제·EC2 terminate·EIP release). 에이전트는 실행 못 함(MFA/SSM/AWS 뮤테이션 분류기 차단) → **모든 명령은 사용자가 맥/호스트에서** 실행.
> 결정(2026-07-30): **① DDL 먼저 적용 후 스냅샷 · ② 타깃 terraform destroy · ③ EIP release**.

## 사전 검증 (이미 확인됨 — 재확인만)

- ✅ **푸시 완료 (2026-07-31)** — `origin/feat/m7-item-selection` = 로컬 HEAD `3994c26`(회귀 테스트 `087fef2` · ADR-0014 `1a28723` · 백로그 `3994c26` 포함, 미푸시 0). 재런치 cold 세션 안전.
  - Phase 1 의 DDL raw URL 이 가리키는 `687c29d` 는 **그대로 유효**(DDL 파일은 이후 무변경).
- ✅ **멱등 DDL 스크립트 존재** — `api/sql/m7-apply-diagnosis-ddl-prod.sql` (IF NOT EXISTS + information_schema 가드 + PRE/POSTFLIGHT). *(런북 §3-C "ALTER 멱등 아님" 경고 해소)*
- ✅ **로컬 tfstate 가 실 리소스를 추적** — `infra/terraform/terraform.tfstate`(serial 257) 에 실 EC2 `i-0eb170169ac70ee05`·RDS `mmt-db`·EIP `eipalloc-02ceb328a11c9b6da` 존재. → **targeted terraform destroy 가 실 리소스에 작동**. *(런북 §2 #2 "state 에 없을 수 있음" 은 이 state 에선 반대)*
- ✅ destroy 차단 없음(`deletion_protection`/`prevent_destroy` 0), remote backend 없음(로컬 state).
- 리소스 ID: EC2 `i-0eb170169ac70ee05` · RDS 식별자 `mmt-db` (엔드포인트 `mmt-db.c7qu444ug8bf.ap-northeast-2.rds.amazonaws.com:3306`) · EIP alloc `eipalloc-02ceb328a11c9b6da` · assoc `eipassoc-082cf1b65b0c6e1e0`.
- **유일한 필수 tf 변수 = `db_password`** (나머지는 default 있음). 프로비저닝 때 쓰던 값 = RDS 마스터 비번.

---

## Phase 0 — 자격 세션 (맥 대화형 터미널)

```bash
cd ~/my-math-teacher/infra/terraform

# 1) 임시자격을 현재 셸에 주입 (반드시 source. MFA 코드 1회 입력, 1h TTL)
source ./tf-assume.sh
#   → "임시자격 export 완료" 나오면 이후 aws/terraform 이 이 자격 사용 (--profile 불요)

# 2) 자격 확인 (env 자격 사용 → 추가 MFA 없음)
aws sts get-caller-identity
#   → Account "<ACCOUNT_ID>", Arn ".../assumed-role/mmt-terraform-admin/terraform" 나와야 정상

# 3) RDS 마스터 비번을 tf 변수로 — 로컬 tfstate 에 평문 저장돼 있어 그대로 로드(화면 미노출)
export TF_VAR_db_password="$(jq -r '.resources[]|select(.type=="aws_db_instance" and .name=="app")|.instances[0].attributes.password' terraform.tfstate)"
[ -n "$TF_VAR_db_password" ] && echo "  db_password 로드됨 (len=${#TF_VAR_db_password})" || echo "  ⚠️ 로드 실패"
```
> 참고: destroy 는 AWS API 로 식별자 삭제라 MySQL 인증 안 함 → 이 값이 틀려도 destroy 는 됨. 다만 tfstate 값 = 실제 프로비저닝 값이라 그대로 쓴다. **Phase 1 DDL 의 mysql 로그인엔 실제 값이 필요** — 호스트 env-file 에서 꺼낸다(Phase 1 참조).

> ⚠️ 1h TTL — 중간에 만료되면 `source ./tf-assume.sh` 재실행. (memory: destroy 중 MFA 만료 주의)

---

## Phase 1 — 결함① DDL 적용 (프로덕션 RDS) — ~~호스트에서~~ **[건너뜀 · 2026-07-31]**

> ⚠️ **건너뜀 결정(2026-07-31):** M7 테이블은 현재 비어 있어(기능 500 으로 데이터 0) 지금 만들 이득이 없고, 멱등 DDL 스크립트가 리포에 커밋돼 있어(`api/sql/m7-apply-diagnosis-ddl-prod.sql`) **재런치 시 적용**한다(§재런치 3). 스냅샷은 DDL 이전 상태로 뜬다. 아래 절차는 재런치 때 그대로 사용.
>
> RDS 는 `publicly_accessible=false` → **맥에서 직접 못 붙는다. EC2 호스트(app SG)에서** 적용.
> 순서상 EC2 는 아직 살아있음(Phase 4 에서 마지막에 destroy). 리포는 공개라 raw URL 로 가져온다.

```bash
# 맥에서 호스트 셸 진입
aws ssm start-session --target i-0eb170169ac70ee05 --profile mmt-admin --region ap-northeast-2
sudo -iu ec2-user      # 세션 안에서

# --- 여기부터 호스트(ec2-user) ---
# RDS 접속 정보는 백엔드 env-file 에 있음 (경로는 환경에 따라 ~/mmt-backend.env 또는 /etc/mmt/backend.env)
grep -iE 'datasource|DB_|MYSQL' ~/mmt-backend.env    # URL/USER/PASSWORD 확인

# 멱등 DDL 스크립트 가져오기 (공개 repo, 배포된 커밋 고정)
curl -fsSL https://raw.githubusercontent.com/data-sy/my-math-teacher/687c29d/api/sql/m7-apply-diagnosis-ddl-prod.sql -o /tmp/m7-ddl.sql

# 적용 (host/user/pass 는 위 env 값으로. DB 는 반드시 mmt)
mysql -h mmt-db.c7qu444ug8bf.ap-northeast-2.rds.amazonaws.com -P 3306 -u mmtadmin -p mmt < /tmp/m7-ddl.sql
```

**GATE 1** — 스크립트 끝의 **[POSTFLIGHT] 6줄이 전부 `OK`** 여야 함. 하나라도 `FAIL` 이면 스냅샷 전에 멈추고 원인 확인(PREFLIGHT 출력으로 부분적용 여부 판단 — 스크립트가 가드로 skip 하므로 재실행 안전).

```bash
rm -f /tmp/m7-ddl.sql   # 정리
exit; exit              # ec2-user → SSM 세션 순서로 빠져나오기
```

---

## Phase 2 — RDS 최종 스냅샷 (데이터 보존, **필수 선행**) — 맥에서

> Phase 1 이후 스냅샷을 떠야 스냅샷이 **M7 스키마를 품는다**(= 재런치 때 DDL 단계 소멸).

```bash
aws rds create-db-snapshot \
  --db-instance-identifier mmt-db \
  --db-snapshot-identifier mmt-mothball-2026-07-31 \
  --region ap-northeast-2

# available 될 때까지 블록 (몇 분)
aws rds wait db-snapshot-available \
  --db-snapshot-identifier mmt-mothball-2026-07-31 --region ap-northeast-2

# 검증: Status=available + 용량 + 생성시각
aws rds describe-db-snapshots \
  --db-snapshot-identifier mmt-mothball-2026-07-31 --region ap-northeast-2 \
  --query 'DBSnapshots[0].[Status,AllocatedStorage,SnapshotCreateTime]' --output table
```

**GATE 2 (하드)** — `Status=available` 를 **두 눈으로 확인하기 전엔 Phase 4(삭제) 절대 진행 금지.** `skip_final_snapshot=true` 라 이 수동 스냅샷이 **유일한 데이터 보존 수단**이다.

---

## Phase 3 — 재런치 상태 캡처 (기록만, 리포 밖 안전한 곳에)

> IaC 밖 상태 — 안 적으면 재런치 때 재구성 불가. **시크릿 값 자체는 리포/이 문서에 쓰지 말 것**(키 이름·타깃만).

```bash
# DNS 현재 타깃 (= 릴리즈될 EIP. 재런치 새 IP 로 이 A레코드 갱신해야 함)
dig +short www.my-math-teacher.com

# 현재 EIP 공인 IP 확인 (기록)
aws ec2 describe-addresses --allocation-ids eipalloc-02ceb328a11c9b6da \
  --region ap-northeast-2 --query 'Addresses[0].[PublicIp,InstanceId]' --output text
```

호스트(SSM)에서 추가로 기록해 둘 것 (값은 안전 보관):
- `~/mmt-backend.env` **키 목록**(JWT/OAuth/DB 시크릿 — 값은 비밀번호 관리처에), `MMT_DIAGNOSIS_ENABLED=true`.
- nginx `active-backend.conf`(upstream fragment) 내용, certs 도메인(`www.my-math-teacher.com`).
- OAuth 콘솔(구글·네이버·카카오) redirect-uri 등록값 — **도메인 기반**이라 DNS 만 새 IP 로 맞추면 재사용 가능.
- 배포 이미지: 프론트 `mmt2024/mmt-front:2.0.0`(Docker Hub 영속). ⚠️ 백엔드 `889390a` 는 **track A 수정 미포함** → 재런치는 `feat/m7-item-selection`(≥`687c29d`)에서 **재빌드**.

---

## Phase 4 — 타깃 terraform destroy (billable 3종만) — 맥에서

> IAM 롤·OIDC provider·SG·keypair(전부 $0, CI/CD 신뢰 기반)는 **건드리지 않는다** → 재런치 마찰 최소.
> 대상 4개: `aws_eip_association.app` · `aws_instance.app` · `aws_eip.app` · `aws_db_instance.app`.

```bash
cd ~/my-math-teacher/infra/terraform

# 4-1) 먼저 destroy PLAN 을 읽는다 (read-only 게이트). use_localstack=false 필수(안 주면 LocalStack 로 감)
terraform plan -destroy \
  -target=aws_eip_association.app \
  -target=aws_instance.app \
  -target=aws_eip.app \
  -target=aws_db_instance.app \
  -var use_localstack=false
```

**GATE 3** — plan 출력이 **정확히 위 4개를 destroy, 그리고 create/change 0** 이어야 한다.
- 리소스 ID 가 `i-0eb170169ac70ee05`·`mmt-db`·`eipalloc-02ceb328…` 와 일치하는지 확인(= state 가 실물과 정합).
- 만약 plan 이 **recreate/drift** 를 보이면 → **멈추고** 원인 확인(누군가 콘솔로 바꿨을 수 있음). 그때는 이 targeted destroy 대신 raw CLI 폴백(맨 아래 부록) 사용.

```bash
# 4-2) 실제 destroy (위 plan 과 동일 타깃. 확인 프롬프트에 yes)
terraform destroy \
  -target=aws_eip_association.app \
  -target=aws_instance.app \
  -target=aws_eip.app \
  -target=aws_db_instance.app \
  -var use_localstack=false
```

---

## Phase 5 — 실제 state 로 소멸 검증 (exit code 말고) — 맥에서

> memory(destroy-verify): tee/exit code 는 못 믿는다. **AWS 실물 조회**로 확인, 멱등 재조회.

```bash
# EC2: terminated/사라짐
aws ec2 describe-instances --instance-ids i-0eb170169ac70ee05 \
  --region ap-northeast-2 --query 'Reservations[].Instances[].State.Name' --output text
# RDS: 사라짐 (DBInstanceNotFound 에러가 정상)
aws rds describe-db-instances --db-instance-identifier mmt-db --region ap-northeast-2 2>&1 | tail -1
# EIP: 사라짐 (InvalidAllocationID.NotFound 가 정상)
aws ec2 describe-addresses --allocation-ids eipalloc-02ceb328a11c9b6da --region ap-northeast-2 2>&1 | tail -1
# tfstate 에서도 4개 빠졌나 (IAM/OIDC/SG/keypair 만 남아야)
terraform state list

# 잔여 비용원 훑기 (Project=mmt 태그로 EBS 스냅샷·미사용 EIP 등)
aws ec2 describe-addresses --region ap-northeast-2 --query 'Addresses[?Tags[?Value==`mmt`]]' --output table
```

**완료 기준** — EC2 없음/terminated · RDS NotFound · EIP NotFound · `mmt-mothball-2026-07-31` 스냅샷 available 잔존 · state 에 IAM/OIDC/SG/keypair 만.
→ **billable 인프라 $0** (스냅샷 스토리지 소액만 과금).

---

## 재런치 런북 (고쳐진 상태로 복구 — 시점 미정)

1. **인프라:** `cd infra/terraform && source tf-assume.sh && export TF_VAR_db_password=… && terraform plan -var use_localstack=false` → 승인 → `terraform apply`. (state 에 IAM/OIDC/SG/keypair 잔존 → EC2·RDS·EIP 4개만 재생성.) **새 EIP → DNS A레코드 갱신.**
2. **RDS 복원:** `mmt-mothball-2026-07-31` 스냅샷에서 restore (또는 새 RDS 를 이 스냅샷으로 생성). **스냅샷에 M7 스키마 포함** → DDL 단계 불요.
3. **호스트 복원:** `~/mmt-backend.env`(시크릿 재주입, `MMT_DIAGNOSIS_ENABLED=true`) · certbot 재발급 · `active-backend.conf` · docker 네트워크 `mmt-net`.
4. **백엔드 재빌드·배포:** `feat/m7-item-selection`(≥`687c29d`, 이상적으론 머지된 main)에서 재빌드한 새 태그. 프론트 `mmt2024/mmt-front:2.0.0` 재사용.
5. **검증:** `bash verify-prod-jwt-isolation.sh` → login 200 → `GET /learning-queues/me` **404/200**(401·500 = 미해결) · 없는 경로 → **404**(마스킹 제거 확증) · 무토큰 → 401.

---

## 부록 — raw CLI 폴백 (GATE 3 에서 drift 나왔을 때만)

```bash
# 스냅샷 available 확인 후 (Phase 2 완료 전제)
aws rds delete-db-instance --db-instance-identifier mmt-db --skip-final-snapshot --region ap-northeast-2
aws ec2 terminate-instances --instance-ids i-0eb170169ac70ee05 --region ap-northeast-2
aws ec2 release-address --allocation-id eipalloc-02ceb328a11c9b6da --region ap-northeast-2
# → 이 경우 tfstate 가 drift 됨. 재런치 전 `terraform state rm` 또는 refresh 로 정리.
```
