# M7 R2 실측 — self-report→DKT 매핑의 실서빙 확률 분포 (대표 4시나리오)

**측정:** 2026-07-14 · 로컬 TF Serving(`mymathteacher/mmt-ai:serving` — **프로덕션 동일 이미지**, model v1 AVAILABLE) · 브랜치 `feat/m7-spec-01-diagnosis`
**목적:** spec-01 §4.4 R2 엣지(빈/얇은/단조 입력의 유효성)와 S4 등급 컷(40/65)의 실서빙 검증 — 사람 게이트 ② 이행
**방법:** `POST /api/v1/diagnosis/preview` 경유(×10 증폭·시퀀스 생성 포함 실경로), 개념 풀 = chapter 442 순회 실개념 8종(8830, 351, 5780, 5781, 2616, 5762, 2068, 2073)

## 핵심 발견 — percent 스케일 단위 불일치 (수정 완료)

- 실서빙 원시 출력 = **0~1 확률**. 구 경로(`ProbabilityService.java:76`)는 원시값을 `probability_percent` 에 그대로 저장.
- 40/65 컷의 선례인 구 ResultView 는 **0~100 스케일 전제**를 주석으로 명시(`ResultView.vue:126` — "런타임 검증에서 스케일·임계 확인")했으나 그 검증(M6 잔여 "TF Serving end-to-end")이 미완이었음.
- 결과: 정규화 전 실측에서 **모든 카드 p<1 → 전원 HIGH, 등급 변별력 0**.
- **수정:** 신규 경로만 `DiagnosisAnalysisService.compute` 에서 모델 출력 ×100 정규화(구 경로 무접촉). 이 문서의 분포는 정규화 후 값.

## 시나리오별 결과 (정규화 후, percent 0~100)

| 시나리오 | 입력 | 결과 |
|---|---|---|
| S1 1답만 | 몰라요 1 (351) | 200 OK, 카드 1: p=15.2 → HIGH. 극소 세션에서도 유효 응답 (구 "최소 크기 3" 우려 반증 — 단 표본 1) |
| S2 전부 알아요 | 알아요 8 | **DKT 호출 생략 확인**, cards=[] + "약점 없음" 헤드라인 = 정상 결과 (§4.4 R2 엣지 규격대로) |
| S3 전부 몰라요 (단조) | 몰라요 8 | 카드 42(확장 포함): p 5.7~54.6, median 17.2 → **HIGH 37 · MID 5 · LOW 0**. 단조 ×10 증폭 입력에서도 스킬별 분포 생존 |
| S4 혼합 | 몰라요 6 + 알아요 2 | 카드 42: p 19.5~58.8, median 40.9 → **HIGH 21 · MID 21 · LOW 0**. 변별력 확보 |

추가 검증: **서빙 활성 상태의 결정론** — preview == submit.result == GET /diagnosis/{id} 전부 True
(2026-07-13 검증 번들의 "fail-soft 한정 동치" caveat 해소. userTestId=90009 로컬).

## S4 컷(40/65) 판단 — 출발값 유지, 재보정은 실사용자 데이터로 (백로그 유지)

- 정규화 후 40/65 는 HIGH/MID 를 실제로 가른다(S4 혼합에서 21/21). **LOW 는 4시나리오 전부 0** —
  "몰라요" 개념·그 선수 확장은 낮은 이해확률이 자연스러워 컷 문제로 단정하지 않음
  (LOW 후보는 "알아요" 근처 개념인데 그건 카드 대상이 아님).
- 65 컷의 실효성(LOW 등급 발생 여부)은 실사용자 다양 입력에서 재관찰 — 컷 상수 2개 조정으로 끝나는 가역 변경.

## 주의 — 로컬 DB 잔재

- 정규화 이전에 저장된 로컬 dev 세션(userTestId 90008)의 probabilities 행은 0~1 원시 스케일.
  프로덕션 미배포(플래그 off)라 실영향 없음. 로컬 재현 시 혼동 주의.
