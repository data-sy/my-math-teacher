# M7 프론트 v2 — 실기기 시각검증 체크리스트 (사람 몫)

> 2026-07-18, 4단계(틸 그로스 검증) 산출물. 헤드리스 390px 캡처 13장(`web-v2/test-results/shots/` — `npx playwright test e2e/capture.spec.ts` 재생성)은 통과 — 아래는 **헤드리스가 대신 못 하는 실기기 확인**만.
>
> 기동: `cd web-v2 && npm run dev` (mock 모드) → 같은 Wi-Fi 실기기에서 `http://<맥IP>:5173` (Vite `--host` 필요 시 추가). 실서버 모드는 `VITE_ENABLE_MOCK=false VITE_API_BASE=http://<맥IP>:8080`.

## 체크 항목

- [ ] **폴드(짧은 뷰포트) 홈**: 헤드라인 + 1차 CTA "무료 진단 시작"이 스크롤 없이 동시 노출되는가 (갤럭시 폴드 커버 화면 등 ~653px 높이)
- [ ] **iOS safe-area**: 노치/다이내믹 아일랜드 기기에서 상단 ‹ 뒤로가기·로그인 링크가 상태바와 겹치지 않는가, 하단 고정 2택 버튼이 홈 인디케이터와 겹치지 않는가 (bb6dda5 패딩 적용분 실기기 확인)
- [ ] **히어로 모션**: 홈 그래프 히어로의 빈 계단 모션이 실기기에서 부드러운가(저사양 프레임 드랍), `prefers-reduced-motion` 설정 시 멈추는가
- [ ] **③ 2택 터치 타깃**: 몰라요/알아요 버튼 연타 시 오탭·더블탭 줌 없는가
- [ ] **⑥ 그래프 제스처**: 핀치 줌·팬·노드 탭이 모바일 브라우저 기본 제스처(뒤로 스와이프 등)와 충돌하지 않는가
- [ ] **④-B 체크리스트 탭**: 항목 탭 → ✓ 반영 체감 지연이 어색하지 않은가 (실패 시 빨간 인라인 스트립이 뜨는지 — 2026-07-18 POC 발견 수정분)
- [ ] **폰트/틸 대비**: 실기기 OLED/저휘도에서 틸 그로스 팔레트 대비(특히 sub 텍스트 `#5F6E67` on paper)가 읽히는가

## 참고 — 자동 검증으로 이미 커버된 것 (재확인 불요)

- 390px 전 화면 레이아웃·토큰 적용 (캡처 13장 시각 확인 완료)
- Cytoscape 상수 ↔ `index.css :root` 동기화 (7종 값 일치 확인)
- 핵심 플로우·undo·약점0·이어가기·폐기 확인·그래프 탐색·404 (mock e2e 9종)
- 실서버 핵심 플로우 ①→②→③→④→귀속→④-B (REAL_SMOKE 스모크 + API 완주 스크립트)
