# M7 프론트 v2 — 실기기 시각검증 체크리스트 (사람 몫)

> 2026-07-18, 4단계(틸 그로스 검증) 산출물. 헤드리스 390px 캡처 13장(`web-v2/test-results/shots/` — `npx playwright test e2e/capture.spec.ts` 재생성)은 통과 — 아래는 **헤드리스가 대신 못 하는 실기기 확인**만.
>
> 기동: `cd web-v2 && npm run dev` (mock 모드) → 같은 Wi-Fi 실기기에서 `http://<맥IP>:5173` (Vite `--host` 필요 시 추가). 실서버 모드는 `VITE_ENABLE_MOCK=false VITE_API_BASE=http://<맥IP>:8080`.

## 체크 항목 (1차 결과 2026-07-18 — 데스크톱 웹 라운드)

- [x] **폴드(짧은 뷰포트) 홈**: 헤드라인 + 1차 CTA "무료 진단 시작"이 스크롤 없이 동시 노출되는가 (갤럭시 폴드 커버 화면 등 ~653px 높이) — *1차 OK*
- [ ] **iOS safe-area**: 노치/다이내믹 아일랜드 기기에서 상단 ‹ 뒤로가기·로그인 링크가 상태바와 겹치지 않는가, 하단 고정 2택 버튼이 홈 인디케이터와 겹치지 않는가 (bb6dda5 패딩 적용분 실기기 확인) — **미확인** (실기기 필요 — dev `--host` LAN 접속으로 재시도)
- [ ] **히어로 모션**: 홈 그래프 히어로의 빈 계단 모션이 실기기에서 부드러운가(저사양 프레임 드랍), `prefers-reduced-motion` 설정 시 멈추는가 — **발견→수정**: 낮은/넓은 뷰포트에서 클러스터 좌측 쏠림·바닥 잘림(? 노드·펄스 클리핑 = "모션 안 보임"의 유력 원인) → 중앙 정렬 래퍼로 수정. 실기기 재확인 필요
- [x] **③ 2택 터치 타깃**: 몰라요/알아요 버튼 연타 시 오탭·더블탭 줌 없는가 — *1차 OK*
- [ ] **⑥ 그래프 제스처**: 핀치 줌·팬·노드 탭이 모바일 브라우저 기본 제스처(뒤로 스와이프 등)와 충돌하지 않는가 — **미확인** (실기기 필요)
- [x] **④-B 체크리스트 탭**: 항목 탭 → ✓ 반영 체감 지연이 어색하지 않은가 (실패 시 빨간 인라인 스트립이 뜨는지 — 2026-07-18 POC 발견 수정분) — *1차 OK*
- [x] **폰트/틸 대비**: 실기기 OLED/저휘도에서 틸 그로스 팔레트 대비(특히 sub 텍스트 `#5F6E67` on paper)가 읽히는가 — *데스크톱 1차 OK, OLED 재확인은 실기기 라운드에서*

### 1차 라운드 추가 발견 (2026-07-18, 데스크톱 웹)

- **⑥/④-B 선택 노드 색**: 경로 하이라이트(초록)와 선택(초록 계열)이 분리 안 됨 → 선택 = 잉크 굵은 링으로 변경 + path+sel 겹침 시 선택이 이기도록 스타일 순서 수정

### 2차 라운드 (2026-07-18 밤, iPhone 실기기 — dev `--host` LAN 접속)

- **히어로 "계단 안 보임"(재보고)**: 클리핑 수정 후에도 지그재그 그래프 배열이 계단으로 안 읽힘 → **디딤판+챌판 계단 프로파일로 재배치**, 빈 계단(?) 디딤판은 빨간 점선 (와이어프레임 01 의 지그재그 원안에서 이탈 — 실기기 피드백 우선). 사용자 재확인 대기
- **⑥ 첫 진입 확대 붕괴**: 검색 입력 폰트 13px → iOS 사파리가 포커스 시 페이지를 16/13≈1.23배 자동 확대하고 복귀 안 함(핀치 축소로만 복귀) → 입력 폰트 16px 로 수정. 실기기 재확인 필요
- safe-area·그래프 제스처·모션 부드러움 = 계속 미확인 (다음 실기기 라운드)
- **히어로 계단형 = 사용자 확정** ("그래프보다 계단형이 훨씬 낫다")
- **⑥ 뎁스 분리감**: depth2 노드가 많으면 덩어리로 보임 → 뎁스 링별 오퍼시티 그라데이션(선택 1 / d1 0.75 / d2 0.45 / 너머 0.2)
- **④-B 체크리스트 재배치**: 페이지 하단 인라인 → 화면 위 상주 플로팅 pill("학습 계단 n/m") + 탭 시 바텀시트 (사용자 확정)
- **✅ 토글 무반응 버그 = 원인 특정·해소(403 스테일 큐)**: 사용자 수거 코드 = 오류 403 → 재귀속으로 큐가 교체된 뒤 옛 화면(캐시 스냅샷)의 queueId 로 PATCH 하던 것 (활성 큐 = 유저당 1개, 새 큐가 대체). 자동 로그인 자체는 정상 동작. 하드닝(실패 시 큐 재조회)으로 1회 실패 → 자가치유 → 재탭 성공 확인, e2e 회귀 고정

## 참고 — 자동 검증으로 이미 커버된 것 (재확인 불요)

- 390px 전 화면 레이아웃·토큰 적용 (캡처 13장 시각 확인 완료)
- Cytoscape 상수 ↔ `index.css :root` 동기화 (7종 값 일치 확인)
- 핵심 플로우·undo·약점0·이어가기·폐기 확인·그래프 탐색·404 (mock e2e 9종)
- 실서버 핵심 플로우 ①→②→③→④→귀속→④-B (REAL_SMOKE 스모크 + API 완주 스크립트)
