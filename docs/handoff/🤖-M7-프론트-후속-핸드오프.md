# 🤖 [MMT] M7 프론트 후속 — 결과·그래프 화면 미완 항목

> **새 세션에 이 파일을 지목하며 "이 핸드오프 시작하자" 라고 말하면 시작한다.**
> 작성: 2026-08-05 · 기준 커밋 = 이 문서가 포함된 커밋 · 브랜치 `feat/m7-item-selection`
> 선행 작업 = [`🤖-M7-재런치-핸드오프.md`](🤖-M7-재런치-핸드오프.md) (프로덕션 재런치 ✅ 완료)
>
> **🟢 2026-08-06 실행 완료 — 아래 §5 결과 요약을 먼저 읽을 것.** §2 의 4건 중 **B·C·D 는 구현·검증 완료**,
> **A 는 [M8] 로 승격**(ROADMAP). 남은 사람 몫 = 와이어프레임 갱신 승인 + 배포. §1~§4 는 당시 진단 기록으로 보존한다.

---

## 0. 전제 — 프로덕션은 정상이다

**`https://www.my-math-teacher.com` 라이브 중이고, 아래 항목들은 장애가 아니라 미완성이다.**

| 구성 | 상태 |
|---|---|
| 백엔드 | `mmt2024/mmt-backend:ea94a1a6836b9ba9b043436fb774ca3b58b3e63c` — A4 결함 ①② 해소 완료 |
| 프론트 | `mmt2024/mmt-front:2.0.1` — 로그아웃 UI 추가분 |
| 인프라 | EC2 + RDS(스냅샷 복원) + EIP + TLS(만료 2026-11-03) · redis · mmt-ai(TF Serving `AVAILABLE`) |
| 익명 진단 플로우 | Playwright 실브라우저 완주 검증 ✅ (API 23건 200 · 콘솔 에러 0) |
| OAuth 로그인 | 3사 정상 (사용자 실계정 확인 완료) |

**재빌드·배포 절차** = [`archive/m7-relaunch-scripts/`](archive/m7-relaunch-scripts/) README.
프론트는 CI 워크플로가 **없다** — 수동 빌드다. ⚠️ **맥은 arm64, EC2 는 x86_64** →
반드시 `docker buildx build --platform linux/amd64`. 안 그러면 `exec format error` 로 죽는다.

```bash
cd web-v2 && docker buildx build --platform linux/amd64 -t mmt2024/mmt-front:<태그> --load .
bash docs/handoff/archive/m7-relaunch-scripts/deployfront.sh <태그>   # 값 갱신 후
```

---

## 1. 이번에 고친 것 (참고)

**홈 인증 상태 행 상시 노출** — `web-v2/src/screens/Home/Home.tsx`

증상: "로그인했는지 알 수가 없고 로그아웃도 못 한다."
원인: 구 규약이 *비로그인 시에만* 로그인 링크를 띄우고, 로그인 상태는 **재진입 배너**가 대신했는데
그 배너는 `queue.items.length > 0` 일 때만 뜬다 → **"로그인 + 저장된 큐 없음" 조합에서 그 자리가 통째로 빈다.**
게다가 로그아웃 진입점이 전 화면에 없어 `AuthContext.logout()` 이 **죽은 코드**였다(백엔드
`DELETE /api/v1/auth/authentication`·API 래퍼까지 전부 구현돼 있는데 호출부만 0).

수정: 인증 행을 상시 노출(비로그인 → 로그인 링크 / 로그인 → 로그아웃 버튼) + 로그아웃 시
`['queue','me']` 캐시 제거. 회귀 테스트 `web-v2/e2e/authrow.spec.ts` 3/3.

⚠️ **미처리 — 승인 대기:** 이 변경은 와이어프레임 `docs/design/v2/01-home.html:157,193` 의
*"로그인 상태면 이 링크 자체가 없음(배너와 상호 배타)"* 규약을 의도적으로 깬다.
**설계 문서는 데이터면이라 자동 수정 금지** → 사용자 승인 후 갱신할 것.

---

## 2. 🔴 미완 항목 4건

### A. 개념 링크(spec-03) 미구현 — **카드에 클릭할 것이 없다**

결과 화면 개념 카드의 설계(`docs/design/v2/04-result.html:281`)는
**개념명 + 시급도 배지 + 사람 언어 근거 + 무료 외부자료 링크(탭 → 새 탭)** 인데,
링크가 항상 빈 배열이다. 라이브 실측:

```json
{ "conceptName": "1부터 5까지의 수", "urgency": "HIGH",
  "blockedDescendants": 109, "links": [] }
```

원인: **`concept_links` 테이블이 프로덕션에 없다**(`information_schema` 확인 = 0).
그게 spec-03 의 산출물인데 ROADMAP 에 *"spec-03(링크) 대기 · 다음 = 프론트 재설계 후 spec-03 층"*
으로 남아 있다 — **설계만 확정(2026-07-13 U1~U6 사인오프)되고 구현이 안 됐다.**

→ 카드의 "왜 급한가"만 살아 있고 **"그래서 지금 뭘 보나"가 통째로 비어 있다.**
사용자가 "카드에 클릭 이벤트 있던 거 아니었어?" 라고 물은 지점이 여기다.

**정본:** [`docs/specs/m7/spec-03-learning-path-links.md`](../specs/m7/spec-03-learning-path-links.md)
(concept_links 테이블 · 병목 상위 30~50 시드 + 결측 허용 · 개념당 링크 3개 · goal=primary).
**규모:** DB 테이블 + 백엔드 조회 + **시드 콘텐츠 작업**(사람 손). 프론트는 이미 `links` 를 렌더한다.

### B. "이걸 모르면 위로 N개 개념이 막혀요" — 지표가 문제

`Result.tsx:190` 이 **조건 없이** 렌더한다. 전체 1,631개 개념의 실측 분포(깊이 3 역방향 폐쇄):

| blocked 수 | 개념 수 | 비율 |
|---|---:|---:|
| **0개** | **577** | **35.4%** |
| 1–4개 | 442 | 27.1% |
| 5–19개 | 417 | 25.6% |
| 20–49개 | 143 | 8.8% |
| 50–99개 | 41 | 2.5% |
| 100개+ | 11 | 0.7% |

**라이브에서 실제로 이렇게 나온다:**
```
"여러 가지 모양 알아보기" → 이걸 모르면 위로 0개 개념이 막혀요
```

문제 4가지:
1. **35%가 0개** — 급하다고 카드에 올려놓고 근거는 "아무것도 안 막음". 무의미를 넘어 역효과
2. **스케일이 널뛴다** — 같은 화면에 2개와 109개가 나란히. 비교 기준이 안 됨
3. **깊이 3 상한이 문구에 안 드러난다** — 109는 "3단계 안에서"인데 전체처럼 읽힘
4. **배지와 근거의 출처가 다르다** — 배지 = DKT 확률, 이 숫자 = 그래프 구조.
   "시급도 중인데 0개 막힘" 조합이 가능

PRD §4.3 의 의도(**절대 점수 대신 사람 언어로 상대 우선순위**)는 타당하다 — **문제는 선택한 지표**다.

**즉시 가능한 최소 개선(프론트만):** `blockedDescendants === 0` 이면 문구를 숨긴다.
**대안 검토:** 숫자 대신 후수 1개를 이름으로 지목("○○을 배우려면 먼저 필요해요")이
학생에게 더 구체적일 수 있다 — 결정 필요.

### C. 학습 경로 그래프의 라벨이 전부 숨겨져 있다

`ConceptGraph.tsx:152-156` — compact 모드에서 **모든 노드 라벨 `text-opacity: 0`, 선택 노드만 `1`.**

```js
// compact(④-B 개요) — 라벨은 선택 노드만
{ selector: 'node',     style: { 'text-opacity': 0 } },
{ selector: 'node.sel', style: { 'text-opacity': 1 } },
```

**버그가 아니라 의도된 동작이다.** 다만 사용자 보고: *"빈 동그라미만 뜨고, 클릭해야 하나씩 보인다."*
"개요"의 목적은 한눈에 구조를 파악하는 건데 아무것도 못 읽는 개요는 개요가 아니다.
전부 0으로 두는 건 과하다(짧은 이름·작은 폰트·상위 N개만 등 중간값이 있다).

### D. 🔴 선결 — 이 그래프가 무엇을 보여줘야 하는가

**C 를 고치기 전에 이게 먼저다.** 현재 ④-B 의 그래프는 **선택 개념 중심 서브그래프**
(`/api/v1/concepts/nodes/{id}` + `edges/{id}`)인데, 저장된 **학습 큐(순서 있는 체크리스트)** 와
어떻게 연결돼야 하는지가 설계에 명확하지 않다.

와이어프레임(`04-result.html:307`)은 순서만 정한다: **④-B = 그래프 → 카드 → 체크리스트**.
"학습 경로 보기"라는 CTA 이름이 약속하는 것과 실제로 보이는 것(중심 서브그래프)이 어긋난다.

→ **사용자에게 물을 것:** 이 그래프가 학습 큐의 순서를 시각화해야 하는가,
아니면 개념 간 관계 탐색용인가. 답에 따라 C 의 라벨 정책도 자동으로 정해진다.

---

## 3. 착수 순서 제안

```
□ D 결정 (그래프의 목적) — 사용자 답변 필요. 이게 C 를 규정한다
□ B 최소 개선: blockedDescendants === 0 이면 문구 숨김 (프론트 1줄, 즉시 배포 가능)
□ C 라벨 정책 조정 (D 확정 후)
□ 01-home.html 와이어프레임 갱신 (§1 승인 대기 항목)
□ A spec-03 착수 여부 결정 — 규모가 커서 별도 마일스톤일 수 있다
```

**B·C 는 프론트만이라 한 번의 재빌드로 묶어 배포할 수 있다.** A 는 백엔드+콘텐츠라 별건.

---

## 4. 검증 자산 (이번 세션에서 만듦)

| 파일 | 용도 |
|---|---|
| `web-v2/e2e/authrow.spec.ts` | 인증 상태 행 회귀 (로컬 mock, 3 테스트) |
| `web-v2/e2e/prod-diagnose.spec.ts` | 라이브 진단 플로우 완주 + 콘솔·네트워크 전수 수집 |
| `web-v2/e2e/prod-login.spec.ts` | 라이브 로그인·OAuth 리다이렉트 정합 |
| `web-v2/playwright.prod.config.ts` | 프로덕션 대상 설정 (dev 서버 안 띄움) |

```bash
cd web-v2
npx playwright test e2e/authrow.spec.ts                          # 로컬 mock
npx playwright test --config=playwright.prod.config.ts           # 라이브
```

⚠️ **mock 모드 토큰은 반드시 `mock-token` 으로 시작해야 한다**(`mocks/handlers.ts:45` 의 `isAuthed`).
아무 문자열이나 넣으면 401 → `client.ts` 가 토큰을 비워(정상 동작) 테스트가 오탐한다.
MSW 는 브라우저 안에서 가로채므로 Playwright `page.route` 스텁으로 우회되지 않는다.

**prod-* 스펙은 임시 진단용**이다 — 계속 둘지 지울지 판단할 것.

---

## 5. 🟢 실행 결과 (2026-08-06)

### 결정 4건 (사용자)

| 항목 | 결정 |
|---|---|
| **D** 그래프의 목적 | **큐 DAG 만 그린다** — 노드 = 저장된 계단 항목만 |
| **B** 근거 문구 | **0개면 숨김 먼저**(프론트), 후수 지목은 별건(백엔드) |
| **A** spec-03 | **[M8] 별도 마일스톤으로 승격** |
| §1 와이어프레임 | **A안 승인 완료(2026-08-06)** — ●7 = 인증 상태 행 상시 노출, 배타 조항 삭제 |

### D·C — 큐 DAG 그래프 (구현 완료)

`04-result.html` ●8 이 이미 *"무너진 토대 → 목표 개념 계단이 그래프 위에 하이라이트"* 라고 요구하고
있었다 — **규약 변경이 아니라 구현이 설계를 못 따라간 것**이었다.

구 구현의 결함(코드 실측): 그래프 중심을 *현재 큐 항목*으로 잡고 `/concepts/nodes/{id}`(그 개념의
**선수** 폐쇄)를 불러왔는데, 큐 노드 집합은 `LearningQueueService:88-100` 에서 **top-N 카드 각각의
선수 폐쇄 합집합**이다 → 현재 항목의 선수가 아닌 큐 항목(뒤쪽 계단·다른 가지)은 **애초에 그래프에
없어서** `pathIds` 가 칠할 대상이 없었다. known 으로 큐에서 뺀 개념은 반대로 그래프에 그대로 떠 있었다.

**조립 방식 (백엔드 무변경):** 카드별 `/concepts/edges/{id}` 합집합을 큐 노드 집합으로 필터.
누락이 없음이 보장된다 — 엣지 쿼리가 `from_concept_id IN (선수 폐쇄)`(= 폐쇄 안 노드로 **들어오는**
간선 전부)라, 두 끝점이 모두 큐에 있는 간선은 반드시 어느 카드의 응답에 포함된다.
노드 라벨은 큐 항목이 이미 갖고 있어 `nodes/{id}` 호출은 **0회**.

⚠️ **방향 규약(실측):** `knowledgeSpaceRowMapper` 가 `source=to_concept_id`·`target=from_concept_id`
로 **뒤집어** 매핑한다. 즉 DB 는 `from=후수 / to=선수`, 와이어는 `source→target = 선수→후수`.

**C 라벨은 D 가 자동으로 규정했다** — 노드가 큐 항목뿐(보통 5~30개)이라 전부 노출. 겹침 대응 4종:
이름 축약(10자) · `spacingFactor` 1.9 · **초기 줌 하한 0.85** · 콘텐츠 경계 패닝 클램프.

> 실측하며 잡은 함정 2개.
> ① **라벨이 간선을 덮는다** — `text-valign: bottom` 이라 세로 DAG 에서 라벨이 간선 자리를 그대로
>    가려 "연결이 없는 것처럼" 보였다(간격을 넓히니 화살표가 드러남).
> ② **`layoutstop` 이 리스너 등록 전에 발화한다** — breadthfirst 는 생성자 안에서 동기로 끝난다.
>    줌 클램프는 인라인 실행해야 하고, `cy.one('layoutstop')` 만 걸면 조용히 안 걸린다.

큐 상태 2종 추가: 완료 = 점선·물러남 / "여기부터" = PRIMARY 링. **새 hue 를 만들지 않고** 기존
어휘만 재사용해 A안 "노드 6 상태 고정" 팔레트를 넓히지 않았다.

### 변경 파일

| 파일 | 내용 |
|---|---|
| `web-v2/src/api/endpoints.ts` | `fetchQueueEdges()` 신설 (카드별 edges 합집합 → 큐 노드 필터) |
| `web-v2/src/components/ConceptGraph.tsx` | compact 라벨 전체 노출 · 줌 하한 · 패닝 클램프 · `doneIds`/`currentId` |
| `web-v2/src/screens/Result/Result.tsx` | ●8 을 큐 DAG 로 배선 · nodeInfo = 계단 번호/상태 · B 0개 숨김 |
| `web-v2/src/screens/Home/Home.module.css` | 로그아웃 버튼 정렬 (2.0.1 배포분 버그) |
| `web-v2/e2e/queue-graph.spec.ts` | 신규 회귀 2건 |
| `web-v2/e2e/flows.spec.ts` | 스테일 카피 단언 갱신 (a0298b0 에서 바뀐 문구) |

**검증:** mock e2e **16/16 green** · 390px 실렌더 캡처로 3상태(초기·선택·완료) 확인 · `tsc --noEmit` 0 · `oxlint` 신규 0.

### 남은 것

- ~~**와이어프레임 갱신 승인**~~ ✅ **완료(2026-08-06)** — `01-home.html` ●7 = 인증 상태 행 상시 노출로 갱신,
  배타 조항 삭제 + 폐기 사유 명기. 같은 서술이 있던 `00-flow-map.html`·`spec-02-react-mvp.md` 도 정합.
  아래는 판단 근거 기록. 조사 결과 배타 조항은 독립 판단이 아니라 파생
  결론이었다: ●7 은 2026-07-17 **05-login-error 검수**에서 *"새 기기·로그아웃 재방문 유저의 저장 큐
  진입로 확보"* 목적으로 신설됐고(커밋 `22beb50`), 로그인 상태면 배너가 그 역할을 하니 **중복 제거**
  차원에서 링크를 뺀 것. 감추려던 게 아니다. 전제(로그인 ⇒ 배너 있음)가 "로그인 + 큐 없음"에서 깨진다.
  그리고 **로그아웃 진입점은 PRD·spec-01/02·와이어프레임 6장 어디에도 없다**(spec-02:45 API 매핑 한 줄뿐)
  — 즉 지킬 만한 의도가 뒤에 없다. 마케팅 원칙(PRD F-4 "1차 CTA 단독 압도")은 **시각 무게** 규칙이라
  12.5px 텍스트 링크인 인증 행과 저촉되지 않는다.
- **배포** — B·C·D 전부 프론트라 한 번의 재빌드로 묶인다(§0 절차, `--platform linux/amd64` 필수).
  2026-08-06: PR [#50](https://github.com/data-sy/my-math-teacher/pull/50) 머지(main `590f6cc`) → `mmt2024/mmt-front:2.0.2`
  빌드 완료(amd64 검증·번들에 이번 변경 포함 확인) → 사용자가 `deployfront.sh 2.0.2` 실행.
  **롤백 = `2.0.1` 태그 원복**(백엔드·DB 무변경).
- **실기기 확인** — 큐가 클 때(13계단) 그래프가 한 화면에 안 들어와 패닝이 필요하다. "전체 보기"
  어포던스를 넣을지는 실기기에서 판단.
- **prod-* 스펙 처분** — 위 §4 미판단 그대로.
